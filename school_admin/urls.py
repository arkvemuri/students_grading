from django.urls import path
from . import views

urlpatterns = [
    # path('student/course/<int:pk>', CourseDetailView.as_view(), name='course-detail'),
    # path('student/course/new', CourseCreateView.as_view(), name='course-new'),
    # path('student/course/delete', CourseDeleteView.as_view(), name='course-delete'),
    # path('student/course/', CourseListView.as_view(), name='course-list'),
]